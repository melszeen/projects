#include <iostream>
 
using namespace std;
 
int main() {
   float age;
 
    cout << "Please enter your age: ";
    cin >> age;
	int aged = age;
	if (age-aged>=0.5)
		cout << "your age is " << aged + 1 << endl;
	else if (cout << "your age is " << aged << endl);
 
}